services:
  db:
    image: postgres:16-alpine
    container_name: immobiliare-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: immobiliare
      POSTGRES_PASSWORD: immobiliare
      POSTGRES_DB: immobiliare
    ports:
      - '5432:5432'
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U immobiliare -d immobiliare']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  db-data:
